function findMissingLetter(arr) {
    let word = arr[0];
    for(let i = 0; i < arr.length; i++){
        word += String.fromCharCode()
    }
}

findMissingLetter(['a','v'])
module.exports = findMissingLetter;
